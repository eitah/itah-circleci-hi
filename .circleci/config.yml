version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            apk --no-cache add curl
            curl 'https://xkcd.com/info.0.json'
            echo 'This is the delivery pipeline'
